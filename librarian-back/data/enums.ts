enum Friendship {
    accepted = 'accepted',
    pending = 'pending',
}

enum BookStatus {
    finished = 'finished',
    inProgress = 'in progress',
    new = 'new'
}


export {
    Friendship,
    BookStatus
}